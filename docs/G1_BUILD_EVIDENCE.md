# G1 Core Build Evidence

Canonical gate: MID-423 / STEP 1 / G1.

This file exists to anchor an inspectable pull-request-triggered GitHub Actions run against the current `main` Core source surface.

Status at creation: **PENDING CI**.

Do not mark G1 PASS until the `.NET 8` restore, Release build with `TreatWarningsAsErrors=true`, and Core smoke harness all succeed.
